agents:
  queue: "default"
  
steps:

  # - label: ":github: Checkout"
  #   command: "git clone https://$GITHUB_TOKEN@github.com/Ali-coder1996/pipeline.git; ls -l"

  - label: ":modal: Build/Push Image"
    command:
      - ls -l
      - buildkite-agent pipeline upload pipeline/template/dev.yaml


  - wait: ~

  - label: ":bash: CleanUp"
    command: "pipeline/script/cleanup.sh"

      
  # - label: "PROD"
  #   command:
  #     - ls -l
  #     - buildkite-agent pipeline upload prod.yaml
  #   plugins:
  #     - 'uber-workflow/run-without-clone':
  #   if: build.branch != "main"
